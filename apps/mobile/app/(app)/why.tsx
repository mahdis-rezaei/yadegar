import { WhyNote } from "../../components/why-note";

// "why I built Yadegar" — the maker's note (shared with the signed-out route).
// AppHeader provides navigation here.
export default function Why() {
  return <WhyNote />;
}
